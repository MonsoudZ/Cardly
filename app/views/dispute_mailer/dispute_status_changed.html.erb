<h2>Dispute status updated</h2>

<p>Hi <%= @user.display_name %>,</p>

<p>The status of dispute #<%= @dispute.id %> has been updated.</p>

<div class="card-details">
  <h3>Dispute Details</h3>
  <div class="detail-row">
    <span class="detail-label">Dispute ID:</span>
    <span class="detail-value">#<%= @dispute.id %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">New Status:</span>
    <span class="detail-value"><%= @dispute.status.titleize %></span>
  </div>
  <div class="detail-row">
    <span class="detail-label">Transaction:</span>
    <span class="detail-value"><%= @transaction.gift_card.brand_name %> Gift Card</span>
  </div>
</div>

<% if @dispute.resolved? %>
  <p><strong>Resolution:</strong> <%= @dispute.resolution_display %></p>
  <% if @dispute.resolution_notes.present? %>
    <blockquote style="border-left: 3px solid #22c55e; padding-left: 16px; margin: 16px 0; color: #4b5563;">
      <%= @dispute.resolution_notes %>
    </blockquote>
  <% end %>
<% end %>

<p style="text-align: center;">
  <%= link_to "View Dispute", dispute_url(@dispute), class: "btn" %>
</p>
